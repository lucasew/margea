# GitHub OAuth App Configuration
#
# Para configurar a autenticação OAuth com GitHub:
# 1. Acesse https://github.com/settings/developers
# 2. Clique em "New OAuth App" (ou "New GitHub App")
# 3. Preencha os dados:
#    - Application name: margea (ou o nome que preferir)
#    - Homepage URL: https://seu-dominio.vercel.app (ou URL de desenvolvimento)
#    - Authorization callback URL: https://seu-dominio.vercel.app/api/auth/callback
#    - (Para desenvolvimento local: http://localhost:5173/api/auth/callback)
# 4. Após criar, copie o Client ID e gere um Client Secret
# 5. Configure essas variáveis abaixo

# Client ID do GitHub OAuth App
GITHUB_CLIENT_ID=

# Client Secret do GitHub OAuth App (NUNCA commitar este arquivo com valores reais!)
GITHUB_CLIENT_SECRET=

# URL de callback (deve corresponder ao configurado no GitHub)
# Desenvolvimento: http://localhost:5173/api/auth/callback
# Produção: https://seu-dominio.vercel.app/api/auth/callback
GITHUB_CALLBACK_URL=

# Secret para assinar JWT das sessões (gere uma string aleatória segura)
# Você pode gerar com: openssl rand -base64 32
SESSION_SECRET=
